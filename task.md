# Checklist Triển khai Đa ngôn ngữ (i18n)

- [x] Lập bộ từ điển (Dictionary) cho tiếng Anh và tiếng Việt trong `main.js`.
- [x] Gắn thẻ `data-i18n="..."` vào các phần tử chữ trong `index.html`.
- [x] Viết hàm `setLanguage(lang)` trong `main.js` để tự động đổi nội dung dựa trên `data-i18n`.
- [x] Lưu tùy chọn ngôn ngữ vào `localStorage` và cập nhật sự kiện click của nút `VN/EN`.
- [x] Kiểm tra kết quả hiển thị thực tế.
